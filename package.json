{
  "dependencies": {
    "chance": "^1.1.11",
    "cypress": "^13.13.0",
    "mocha": "^10.6.0"
  },
  "scripts": {
    "start": "npx concurrently \"cd back/functions && npm install && npm run serve\" \"cd front && npm install && ng serve\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
